// Example Database 2 - User Domain
// Đây là template cho database thứ 2 trong tương lai

model User {
  id        String   @id @default(cuid())
  email     String   @unique
  name      String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("users")
}
